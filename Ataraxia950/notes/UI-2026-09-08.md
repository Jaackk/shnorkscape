# Everyday interface controls, 2026-09-08

The user's priority was missing UI elements and ordinary controls that did
nothing, especially running and the world map. This pass preserves the Sep7
multiplayer/admission, scheduling, cache-valid NPC spawns, 29 skills and saved
experience work while adding and validating the following controls.

## Installed and verified

- Run: native1465:14 routes to the original1465:11 run handler. The orb visibly
  toggles and the measured movement changes from one to two tiles per tick.
  Verified script3916 removes unsupported Rest from the menu while retaining
  Run's existing prediction hook.
- Ribbon: native1431 at1477:64 offers Skills, Backpack, Equipment and All Chat.
  Its domain2 client varbits select those shortcuts; native cache scripts own
  panel toggling. All four close/reopen correctly after a fresh login.
- Panel layout: eight inactive panels are hidden with nonzero100x135 cache
  minimum rectangles. All12 relevant live tab bars are reset before saving
  working9/preset8. This prevents empty Private Chat or unrelated panel
  promotion through stale default links. Fresh-login, map round trip and
  maximize/restore checks all passed.
- World map:1465:10 opens viewport1421 at1477:31 and controls1422 at1477:37,
  with the scene attachment at30 temporarily removed. Client variables622/674
  center the map and locate the player. Dragging, zoom buttons, wheel, resizing,
  X close, Escape close and centered reopening passed native checks. Close
  cleans up both map attachments and restores scene1482 at30.
- CLOSE_MODAL retires bank state, cancels bank approaches and closes the map.
  Stale withdrawals are refused even with the inherited close-interface lock.
- Unsupported interface actions retain diagnostic counts without chat spam.
- Modern interface/combat/skin bits27169/27168/22875 are cleared before panel
  attachments. The original bootstrap fixture remains85 packets and the
  modern one97; the separate ribbon initialization is64 packets.

Start-Server.ps1 enables `ataraxia947.worldMap`; its code default remains false.
`ataraxia947.ribbon=false` disables the new ribbon. Local ribbon configuration
is initialized at each login; editable saved ribbon preferences are future work.

## Final build and evidence

Full Ship passed **484 engine +68 frontend tests (552 total)** with no failures,
errors or skips, installed both projects, and restarted server **PID18244** at
2026-09-08T17:29:25Z. The installed Java25 distribution also passed the map
lifecycle and SHA-pin smoke against the selected flat cache.

- Build: `logs/ui-2026-09-08-accepted-ship.log`.
- Installed map smoke: `logs/ui-world-map-accepted-installed-smoke.log`.
- Run/bank encrypted-input smoke: `Ataraxia947/build/ui-input-close-run-smoke.log`.
- Rollback snapshot: `backups/install-2026-09-08-122850` (112 jars).
- Acceptance: `OpenNXT/data/prot/947/generated/native947-3/verified/ui/UI-ACCEPTANCE.md`.
- Final runtime: `logs/ui-debug/result-d7b5b5af-344c-4125-9288-2e4f9745e559.txt`.

After the user's manual login, all four panels passed close/reopen. The map
opened correctly and wheel zoom changed75% to100%. Maximizing with the map open,
closing it, toggling Chat, restoring the smaller window and toggling Chat again
never revealed an inactive chat panel. The user separately confirmed map dragging
and the visible view moved from Varrock to Brimhaven. Final runtime reported an
active session, map closed, running=true, energy100, and zero tick failures,
strict facade hits or scheduler failures. The client was left in the world with
all four supported panels open at856x511.

## Debugging findings retained for future ports

The old viewport at30 drew above its static children, hiding controls37.
Overlay50 then failed because layout initialization hides it and its dynamic
dimmer blocks input. Using sibling31/37 avoids both problems. Bounded local
JVM redefinition tested map candidates without another authentication cycle;
the final full Ship installed the accepted source, so no hot patch is needed
on restart. The final snapshot only read health counters.

The first tab repair cleared links but saved zero-size hidden rectangles.
Cache8701 treats that state as unset and recursively restores preset1, including
its default chat links. A settled-session repair worked because those rectangles
already had nonzero sizes. The final bootstrap now explicitly avoids that
sentinel. No arbitrary delay or map-specific tab cleanup was added.

Detailed cache pins, script proofs and native outcomes live in `WORLD_MAP.md`,
`RIBBON.md` and `RUN_ORB_MENU.md` beside the acceptance report.

## Remaining work

The broader41-panel migration is not complete. Management menus, spellbooks,
prayers, social panels, dialogues and server-owned map travel need their own
ports. Equipment retains its known duplicate-title/geometry limitation and
force-open fallback. Music interface187 text drops and unrelated unhandled
frames remain diagnostic work; the final snapshot had six facade drops and82
unhandled frames. The earlier read-only mode observer did not establish layout
bit38842. The inherited910 map subpanel698 is a947 membership upsell and is not
attached.
