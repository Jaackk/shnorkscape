# Native950 logout opcode202

The paired original client proves that opcode202 has a zero-byte payload and dispatches to a full-session reset routine. The server must write the complete encrypted frame and wait for flush completion before closing the socket. The renderer still needs live confirmation that it visibly returns to login.

Client: OpenNXT/data/clients/950/win64/original/rs2client.exe
SHA256: fc7492548627a4068e88a7a45ba4f0be93004adf246bb4b174b24ae588768b36

The registrar at0x14000e930..0x14000e948 sets size0 inR8D, opcode0xCA(202) inEDX, and descriptor0x140e98e10 inRCX. The handler binding at0x1400fdba2..0x1400fdbd8 installs vtable0x140b5c178 against descriptor+0x10. Vtable offset16 is0x1401052f0: it adds8 tothis and jumps to0x1400fe2b0.

The parser0x1400fe2b0..0x1400fe4c9 does not consume packet data. Its incomingRDX is replaced with client state before being used. It retires connection/buffer queues, resets login-attempt fields, calls the state setter with10 and then0, clears additional world/input state, and calls login-context cleanup with flags0 and an empty string. The state setter0x140026b00..0x140026b66 is independently identified by its observer notifications and assignment tocontext+0x19fa0.

Parser SHA256: cd4a8598379deb65e18c0564f253546d8a73e59d4400b45e381f094f4b4ab166.
Dispatch thunk SHA256: fe6491d24901357026441ce69d90ddafdcaac8e4b471708a5442f9501286f844.

Opcode73 is different: its one-byte parser0x140109100..0x1401091de reads a plain unsigned reason byte and stores it in client state, then runs the state10/reset subset. It must not be substituted for the empty202 response. Its parser SHA256 is5cd5b0b9c88de2d55538f3c5c3c680a6d398ee5fe830fc2135f5828f4c996997.

Run tools/verify_native950_logout.py for the read-only binary assertions; optional --output writes the derived evidence JSON. protocol-analysis/logout-202-950-evidence.json records exact native ranges, hashes, instructions, descriptor/vtable addresses, and semantic limits.

These semantics support full logout. They do not establish a return-to-lobby transfer packet, a process-quit command, or that a particular stored username field is cleared. The latter should be described only as login-context cleanup until the helper's whole data model is independently traced.
